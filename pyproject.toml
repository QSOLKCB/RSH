[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rsh-geometry"
version = "4.0.0"
description = "Bounded Frenet-Serret geometry, deterministic evidence, epistemic governance, cross-runtime conformance, and Lean 4 formal verification"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MPL-2.0"}
authors = [
  {name = "Dr. J. Robitaille"},
  {name = "Trent Slade", email = "qsolimc@users.noreply.github.com"},
]
keywords = ["geometry", "frenet-serret", "deterministic", "verification", "epistemic-governance", "conformance", "formal-verification", "lean4", "helix", "tissue-simulation", "functional-cohesion", "constitutional-governance", "rust-tissue", "tissue-conformance", "webassembly", "webgpu", "wgsl", "ffi", "cpp", "cuda", "npu", "hardware-validation", "lie-group-integration", "fuzz-testing", "gpu-path-integration"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "License :: OSI Approved :: Mozilla Public License 2.0 (MPL 2.0)",
  "Operating System :: OS Independent",
]
dependencies = []

[project.scripts]
rsh = "rsh.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
